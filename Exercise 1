#include <bits/stdc++.h>
using namespace std;
int largest(vector<int> arr)
{
    int n=arr.size();
    int c=arr[0];
    for(int i=0;i<n;i++)
    {
        if(arr[i]>=c)
        {
            c=arr[i];
        }
    }
    return c;
}
